# ------------------------------------------------------------------
# Supabase (Project Settings -> API)
# ------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL="https://<project-ref>.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="<your-anon-key>"

# ------------------------------------------------------------------
# Database (Project Settings -> Database -> Connection string)
# DATABASE_URL: transaction pooler (port 6543), used by the app at runtime.
# DIRECT_URL:   direct connection (port 5432), used by Prisma migrations.
# ------------------------------------------------------------------
DATABASE_URL="postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres"

# ------------------------------------------------------------------
# AI providers - configure at least one.
# AI_PROVIDER picks the default ("openai" or "anthropic"); users can
# override it per-account in Settings.
# ------------------------------------------------------------------
AI_PROVIDER="openai"
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-4o-mini"
ANTHROPIC_API_KEY=""
ANTHROPIC_MODEL="claude-3-5-haiku-latest"

# ------------------------------------------------------------------
# Notifications
# ------------------------------------------------------------------
# Absolute URL of the deployed app, used for links in notification emails.
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Email channel (Resend, https://resend.com). Optional: when missing,
# email sends are logged and skipped; in-app notifications still work.
RESEND_API_KEY=""
EMAIL_FROM="FinPilot <notifications@yourdomain.com>"

# Web Push (VAPID). Optional. Generate a key pair once with:
#   npx web-push generate-vapid-keys
VAPID_SUBJECT="mailto:you@yourdomain.com"
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""

# Bearer token protecting GET /api/cron/notifications. Vercel Cron sends
# it automatically when CRON_SECRET is set in the project's env vars.
CRON_SECRET=""
